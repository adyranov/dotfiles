{{- /* -------------------- Basic User / Environment -------------------- */ -}}
{{- $personalUsername := "change_me" -}}
{{- $personalName := "Change Me" -}}
{{- $personalEmail := "change.me@example.com" -}}
{{- $restricted := false -}}
{{- $work := false -}}
{{- $gpg := dict "sign" "" "encrypt" "" -}}
{{- $secrets := dict "age" stdinIsATTY "bitwarden" false -}}
{{- $toolchains := list "ai" "cloud" "docker" "golang" "iac" "java" "kubernetes" "node" "python" "rust" "extra" -}}
{{- $agents := list "pi" -}}

{{- $data := . -}}
{{- range $k := list "personal" "gpg" "host" "toolchains" -}}
  {{- $_ := set $data $k (default (dict) (get $data $k)) -}}
{{- end -}}

{{- /* -------------------- OS / Distro -------------------- */ -}}
{{- $distro := dict "family" .chezmoi.os "id" (get .chezmoi.osRelease "id" | default .chezmoi.os) -}}
{{- if or (eq $distro.id "arch") (eq $distro.id "archarm") -}}{{- $_ := set $distro "id" "archlinux" -}}{{- end -}}
{{- $supported := list "darwin" "ubuntu" "archlinux" "fedora" -}}
{{- $isSupported := false -}}
{{- range $supported -}}{{- if eq . $distro.id -}}{{- $isSupported = true -}}{{- end -}}{{- end -}}
{{- if not $isSupported -}}
  {{- writeToStdout (printf "⚠️ Unsupported distribution: %s\n" $distro.id) -}}
  {{- exit 1 -}}
{{- end -}}

{{- /* -------------------- Ephemeral Environment -------------------- */ -}}
{{- $ephemeral := false -}}
{{- $ephemeralReasons := list -}}

{{- if stat "/.dockerenv" -}}
  {{- $ephemeral = true -}}
  {{- $ephemeralReasons = append $ephemeralReasons "Detected /.dockerenv" -}}
{{- end -}}
{{- if stat "/run/.containerenv" -}}
  {{- $ephemeral = true -}}
  {{- $ephemeralReasons = append $ephemeralReasons "Detected /run/.containerenv" -}}
{{- end -}}

{{- if stat "/proc/1/cgroup" -}}
  {{- $cgroup := output "cat" "/proc/1/cgroup" -}}
  {{- $match := regexFind "(docker|lxc|podman|kubepods|containerd)" $cgroup -}}
  {{- if $match -}}
    {{- $ephemeral = true -}}
    {{- $ephemeralReasons = append $ephemeralReasons (printf "/proc/1/cgroup matched %s" $match) -}}
  {{- end -}}
{{- end -}}

{{- if stat "/proc/1/mountinfo" -}}
  {{- $mountinfo := output "cat" "/proc/1/mountinfo" -}}
  {{- $match := regexFind "(overlay|containers)" $mountinfo -}}
  {{- if $match -}}
    {{- $ephemeral = true -}}
    {{- $ephemeralReasons = append $ephemeralReasons (printf "/proc/1/mountinfo matched %s" $match) -}}
  {{- end -}}
{{- end -}}

{{- $isWSL := or (env "WSL_DISTRO_NAME") (env "WSL_INTEROP") (and (eq $distro.family "linux") (contains (lower (output "uname" "-r")) "microsoft")) -}}
{{- if and $ephemeral $isWSL -}}
  {{- $ephemeral = false -}}
  {{- $ephemeralReasons = list -}}
{{- end -}}

{{- if $ephemeral -}}
  {{- writeToStdout "🛳️ Running in a containerized (ephemeral) environment.\n" -}}
  {{- range $reason := $ephemeralReasons -}}
    {{- writeToStdout (printf "   • %s\n" $reason) -}}
  {{- end -}}
{{- end -}}

{{- /* -------------------- Brew Path -------------------- */ -}}
{{- $brewPath := "" -}}
{{- if eq $distro.family "darwin" -}}
  {{- $brewPath = "/usr/local/bin/brew" -}}
  {{- if eq .chezmoi.arch "arm64" -}}
    {{- $brewPath = "/opt/homebrew/bin/brew" -}}
  {{- end -}}
{{- else -}}
  {{- $brewPath = "/home/linuxbrew/.linuxbrew/bin/brew" -}}
{{- end -}}

{{- /* -------------------- Chassis / Host Type -------------------- */ -}}
{{- $chassisType := "desktop" -}}
{{- if $ephemeral -}}
  {{- $chassisType = "ephemeral" -}}
{{- else if eq $distro.family "linux" -}}
  {{- if $isWSL -}}
    {{- $chassisType = "wsl" -}}
  {{- else -}}
    {{- $hostnamectl := output "hostnamectl" "--json=short" -}}
    {{- $parsed := mustFromJson $hostnamectl -}}
    {{- $chassis := $parsed.Chassis | default "desktop" -}}
    {{- $chassisType = $chassis -}}
  {{- end -}}
{{- else if eq $distro.family "darwin" -}}
  {{- $hwModel := output "sysctl" "-n" "hw.model" -}}
  {{- if contains "MacBook" $hwModel -}}
    {{- $chassisType = "laptop" -}}
  {{- else -}}
    {{- $chassisType = "desktop" -}}
  {{- end -}}
{{- else if eq $distro.family "windows" -}}
  {{- $psCmd := "if (Get-WmiObject -Class win32_battery) { 'laptop' } elseif ((Get-WmiObject -Class Win32_SystemEnclosure).ChassisTypes -contains 8) { 'laptop' } else { 'desktop' }" -}}
  {{- $chassisType = output "powershell.exe" "-noprofile" "-command" $psCmd -}}
{{- end -}}

{{- /* -------------------- Toolchain Defaults -------------------- */ -}}
{{- $toolchainsEnabled := dict -}}
{{- $toolchainsSelected := list -}}
{{- range $t := $toolchains -}}
  {{- $with := (or (eq (env (printf "WITH_%s" (upper $t)) | lower) "1")
                   (eq (env (printf "WITH_%s" (upper $t)) | lower) "true")) -}}

  {{- $withoutAll := (or (eq (env "WITHOUT_TOOLCHAINS" | lower) "1")
                         (eq (env "WITHOUT_TOOLCHAINS" | lower) "true")) -}}

  {{- $without := (or (eq (env (printf "WITHOUT_%s" (upper $t)) | lower) "1")
                      (eq (env (printf "WITHOUT_%s" (upper $t)) | lower) "true")) -}}

  {{- $_ := set $toolchainsEnabled $t (or $with (and (not (or $withoutAll $without)) true)) -}}
{{- end -}}

{{- /* -------------------- Interactive Prompts -------------------- */ -}}
{{- $interactive := true -}}
{{- if stdinIsATTY -}}
  {{- $work = promptBoolOnce $data.host "work" "🏢 Work Environment" $work -}}
  {{- $restricted = promptBoolOnce $data.host "restricted" "🔒 Restricted Environment" $restricted -}}
  {{- $personalUsername = promptStringOnce $data.personal "username" "👤 Personal Git Username" $personalUsername -}}

  {{- if eq $personalUsername "adyranov" -}}
    {{- $personalName = "Artem Dyranov" -}}
    {{- $personalEmail = "1038141+adyranov@users.noreply.github.com" -}}
    {{- if not $work -}}
      {{- $_ := set $gpg "sign" "0xAE12214C66692D96" -}}
      {{- $_ := set $gpg "encrypt" "0xE770F55D35040481" -}}
    {{- end -}}
  {{- end -}}

  {{- $personalName = promptStringOnce $data.personal "name" "📝 Personal Git Name" $personalName -}}
  {{- $personalEmail = promptStringOnce $data.personal "email" "📧 Personal Git Email" $personalEmail -}}

  {{- $toolchainIcons := dict
      "ai"          "🤖"
      "cloud"       "☁️"
      "docker"      "🐳"
      "golang"      "🐹"
      "iac"         "🏗️"
      "java"        "☕"
      "kubernetes"  "☸️"
      "node"        "🟢"
      "python"      "🐍"
      "rust"        "🦀"
      "extra"       "✨"
  -}}

  {{- /* Build display names */ -}}
  {{- $toolchainsDisplay := list -}}
  {{- range $t := $toolchains -}}
    {{- $toolchainsDisplay = append $toolchainsDisplay (printf "%s %s" (get $toolchainIcons $t) $t) -}}
  {{- end -}}

  {{- $toolchainsSelected = promptMultichoiceOnce
      $data
      "toolchainsSelected"
      "⚙️ Select toolchains to enable"
      $toolchainsDisplay
  -}}

  {{- range $t := $toolchains -}}
    {{- $_ := set $toolchainsEnabled $t false -}}
  {{- end -}}
  {{- range $s := $toolchainsSelected -}}
    {{- range $t := $toolchains -}}
      {{- if eq $s (printf "%s %s" (get $toolchainIcons $t) $t) -}}
        {{- $_ := set $toolchainsEnabled $t true -}}
      {{- end -}}
    {{- end -}}
  {{- end -}}

  {{- if and (get $toolchainsEnabled "kubernetes") (not (get $toolchainsEnabled "docker")) -}}
    {{- $_ := set $toolchainsEnabled "docker" true -}}
    {{- writeToStdout "🐳 Enabled Docker for Kubernetes.\n" -}}
  {{- end -}}

{{- writeToStdout "💡 Tip: You can re-enter your name and email with `chezmoi init --data=false`.\n" -}}
{{- else -}}
  {{- $interactive = false -}}
  {{- writeToStdout "🤖 Running in a non-interactive environment.\n" -}}
{{- end -}}

{{- /* -------------------- AI agent toolchains (nested under ai) -------------------- */ -}}
{{- $agentIcons := dict
    "pi"       "🥧"
-}}
{{- $agentsSelected := list -}}
{{- if stdinIsATTY -}}
  {{- if get $toolchainsEnabled "ai" -}}
    {{- $agentsDisplay := list -}}
    {{- range $a := $agents -}}
      {{- $agentsDisplay = append $agentsDisplay (printf "%s %s" (get $agentIcons $a) $a) -}}
    {{- end -}}
    {{- $agentsSelected = promptMultichoiceOnce
        $data
        "agentsSelected"
        "🤖 Select AI agents to enable"
        $agentsDisplay
    -}}
    {{- range $a := $agents -}}
      {{- $_ := set $toolchainsEnabled $a false -}}
    {{- end -}}
    {{- range $s := $agentsSelected -}}
      {{- range $a := $agents -}}
        {{- if eq $s (printf "%s %s" (get $agentIcons $a) $a) -}}
          {{- $_ := set $toolchainsEnabled $a true -}}
        {{- end -}}
      {{- end -}}
    {{- end -}}
  {{- else -}}
    {{- range $a := $agents -}}
      {{- $_ := set $toolchainsEnabled $a false -}}
    {{- end -}}
  {{- end -}}
{{- else -}}
  {{- range $a := $agents -}}
    {{- $with := (or (eq (env (printf "WITH_%s" (upper $a)) | lower) "1")
                     (eq (env (printf "WITH_%s" (upper $a)) | lower) "true")) -}}
    {{- $without := (or (eq (env (printf "WITHOUT_%s" (upper $a)) | lower) "1")
                        (eq (env (printf "WITHOUT_%s" (upper $a)) | lower) "true")) -}}
    {{- $aiOn := get $toolchainsEnabled "ai" -}}
    {{- $_ := set $toolchainsEnabled $a (or $with (and $aiOn (not $without))) -}}
  {{- end -}}
{{- end -}}

{{- $anyAgentOn := false -}}
{{- range $a := $agents -}}{{- if get $toolchainsEnabled $a -}}{{- $anyAgentOn = true -}}{{- end -}}{{- end -}}
{{- if and $anyAgentOn (not (get $toolchainsEnabled "ai")) -}}
  {{- $_ := set $toolchainsEnabled "ai" true -}}
  {{- writeToStdout "🤖 Enabled ai umbrella (required by an enabled agent).\n" -}}
{{- end -}}

{{- if get $toolchainsEnabled "pi" -}}
  {{- if not (get $toolchainsEnabled "node") -}}
    {{- $_ := set $toolchainsEnabled "node" true -}}
    {{- writeToStdout "🟢 Enabled Node.js for pi (npm install).\n" -}}
  {{- end -}}
{{- end -}}

hooks:
  read-source-state:
    pre:
      command: /usr/bin/env
      args:
        - -S
        - bash
        - -euo
        - pipefail
        - {{ joinPath .chezmoi.sourceDir (printf ".chezmoiscripts/os/%s/.init.sh" $distro.id) | toToml }}

{{- if $secrets.age }}
encryption: age
age:
  command: "rage"
  identity: "{{ $.chezmoi.homeDir }}/.config/age/chezmoi.txt"
  recipient: "age16kxd4ljclq9ksnxvl2ee7a5xnj744kwyv04p04ka0n3rzxdpl5nsq52svl" # pragma: allowlist secret
{{- end }}
gitHub:
  refreshPeriod: "168h"
status:
  exclude: ["always"]
data:
  brew:
    path: "{{ $brewPath }}"
  personal:
    email: "{{ $personalEmail }}"
    name: "{{ $personalName }}"
    username: {{ $personalUsername }}
  gpg:
    sign: "{{ $gpg.sign }}"
    encrypt: "{{ $gpg.encrypt }}"
  host:
    arch: "{{ .chezmoi.arch }}"
    distro:
      family: "{{ $distro.family }}"
      id: "{{ $distro.id }}"
    home: "{{ .chezmoi.homeDir }}"
    source: "{{ .chezmoi.sourceDir }}"
    type: "{{ $chassisType }}"
    work: {{ $work }}
    interactive: {{ $interactive }}
    restricted: {{ $restricted }}
  secrets:
    age: {{ $secrets.age }}
  toolchains:
  {{- range $t, $enabled := $toolchainsEnabled }}
    {{ $t }}: {{ $enabled }}
  {{- end }}
  toolchainsSelected:
  {{- range $i, $t := $toolchainsSelected }}
    - "{{ $t }}"
  {{- end }}
  agentsSelected:
  {{- range $i, $a := $agentsSelected }}
    - "{{ $a }}"
  {{- end }}
