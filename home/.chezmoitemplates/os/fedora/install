
echo "🛠️ Refreshing DNF metadata and installing packages…"
{{- if or (eq .host.type "ephemeral") .host.interactive }}
sudo dnf -y upgrade --refresh
{{- end }}
sudo dnf -y install --setopt=install_weak_deps=False {{ includeTemplate "common/packages/render" (dict "root" .) }}
echo "✅ DNF packages ensured."
