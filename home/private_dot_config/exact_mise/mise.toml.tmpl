[tools]
'usage' = 'latest'
{{- includeTemplate "common/helpers/render-packages" (dict "root" . "packageManager" "mise" "render" "mise") }}

[settings]
gpg_verify = true
paranoid = true
trusted_config_paths = ["{{ .host.source | dir }}"]
