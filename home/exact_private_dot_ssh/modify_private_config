{{- /* chezmoi:modify-template */ -}}
{{- if (eq .host.distro.family "darwin") -}}
Include {{ .host.home }}/.colima/ssh_config
{{- end }}
Include {{ .host.home }}/.config/ssh/config

{{- if regexMatch "(?m)^#### dotfiles:unmodified" .chezmoi.stdin }}
{{ regexFind "(?s)#### dotfiles:unmodified.*" .chezmoi.stdin }}
{{- else }}
{{ print "#### dotfiles:unmodified\n" }}
{{- end -}}
